<UserControl x:Class="DragonEnvelopes.Desktop.Controls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Border CornerRadius="16"
            Padding="18"
            Background="{StaticResource PanelBackgroundBrush}"
            BorderBrush="{StaticResource PanelOutlineBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="{Binding Header, ElementName=Root}" Style="{StaticResource HeadingTextStyle}" FontSize="19" />
                <TextBlock Margin="0,6,0,0"
                           Text="{Binding Subheader, ElementName=Root}"
                           Style="{StaticResource CaptionTextStyle}" />
            </StackPanel>

            <ContentPresenter Grid.Row="1" Margin="0,18,0,0" />

            <Border Grid.RowSpan="2"
                    Background="#A20B1220"
                    CornerRadius="12"
                    Visibility="{Binding IsLoading, ElementName=Root, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Width="180" Height="8" IsIndeterminate="True" />
                    <TextBlock Margin="0,12,0,0"
                               Text="Loading data..."
                               Style="{StaticResource BodyTextStyle}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

            <Border Grid.RowSpan="2"
                    Background="#A20B1220"
                    CornerRadius="12">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty, ElementName=Root}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsLoading, ElementName=Root}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380">
                    <TextBlock Text="{Binding EmptyMessage, ElementName=Root}"
                               Style="{StaticResource BodyTextStyle}"
                               TextAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
