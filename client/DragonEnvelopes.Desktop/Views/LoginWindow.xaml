<Window x:Class="DragonEnvelopes.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DragonEnvelopes.Desktop.Controls"
        mc:Ignorable="d"
        Title="DragonEnvelopes Sign In"
        Width="560"
        Height="620"
        MinWidth="520"
        MinHeight="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource ShellBackgroundBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,6,0,20">
            <Image Width="260"
                   Height="120"
                   Stretch="Uniform"
                   Source="/Assets/dragonenvelopes-logo-transparent.png" />
        </StackPanel>

        <controls:LoginControl x:Name="LoginControl"
                               Grid.Row="1" />

        <TextBlock Grid.Row="2"
                   Margin="0,14,0,0"
                   HorizontalAlignment="Center"
                   Style="{StaticResource CaptionTextStyle}"
                   Text="Native login mode enabled: no browser redirect." />
    </Grid>
</Window>
